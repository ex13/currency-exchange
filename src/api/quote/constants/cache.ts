/**
 * Default TTL for the cache: 60 seconds.
 */
export const DEFAULT_CACHE_TTL = 1000 * 60;

/**
 * Default cache capacity (the max amount of elements in the memory).
 */
export const DEFAULT_CACHE_CAPACITY = 2;
